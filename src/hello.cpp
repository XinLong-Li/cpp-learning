#include <iostream>

/**
 * C++ 学习之旅从这里开始！
 *
 * 编译 & 运行：
 *   方法一（VS Code）：Cmd+Shift+B 构建, F5 调试运行
 *   方法二（终端）：先 cmake -S . -B build，再 cmake --build build
 *                   然后 ./build/bin/hello
 */
int main() {
    std::cout << "Hello, C++!" << std::endl;
    std::cout << "Let's start learning C++20!" << std::endl;
    return 0;
}
